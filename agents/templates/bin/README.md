# Bin Templates

Reusable command-wrapper templates live here.

## Templates

- `dev-config-template.md` - local `dev.config.rb` overrides for the shared Ruby `marlens-dev` Docker Compose wrapper.
- `dev-shim-template.md` - project-local `bin/dev` shim that delegates to the shared `marlens-dev` binary.
