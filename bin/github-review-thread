#!/usr/bin/env node
import process from 'node:process';

import { runCli } from '../lib/github-review-thread/run-cli.js';

process.exitCode = await runCli(process.argv.slice(2), process.cwd(), process.env);
