---
name: worktree-creation
description: Use when starting issue work in a separate Git worktree with the standard layout and local environment setup.
---

# Worktree Creation

Use the local `docs/workflows/worktree-creation-workflow.md`, then `agents/workflows/worktree-creation-workflow.md`; otherwise use the [packaged workflow](workflow.md): read `skill://worktree-creation/workflow.md` before creating an issue worktree.
