---
name: feature
description: Use for user-facing feature work that should flow through an issue, branch, pull request, verification, release, and install check.
---

# Feature Workflow

Use the local `docs/workflows/feature-workflow.md`, then `agents/workflows/feature-workflow.md`; otherwise use the [packaged workflow](workflow.md).
