# Title Casing Reference

Authoritative source: `agents/references/title-casing-reference.md`.

Read the authoritative reference when it is available. This packaged file is a thin OMP `skill://pull-request-management/title-casing.md` and Claude Code `${CLAUDE_SKILL_DIR}/title-casing.md` fallback for agents that cannot directly read the package-level `agents/` reference.

Minimum fallback contract: write PR titles in title case, while preserving acronyms, intentional all-caps terms, exact code identifiers, package names, and command names.
