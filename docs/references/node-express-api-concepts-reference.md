# Node Express API Concepts Reference

Compatibility reference for Node.js + Express API concepts.

Use `docs/references/express-light-rail-backend-concepts-reference.md` for the centralized backend rail model: route, controller/handler, policy/auth check, service/use case, serializer, model/repository, and factory/fixture.

This file remains so older references to Node Express API guidance still resolve, but the Express Light Rail reference is authoritative.
