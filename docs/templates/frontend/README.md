# Frontend Templates

Reusable frontend implementation templates live here. Keep framework-specific families in subdirectories when a pattern depends on a component library or route/query helper.

## Templates

- `api-typescript-template.md` - TypeScript API client shape aligned to backend models, serializers, policies, and query options.
